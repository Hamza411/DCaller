import { StyleSheet } from "react-native";
import { COLORS } from '../../../assets/constant/index'

export default StyleSheet.create({

    bg: {
        flex: 1,
        backgroundColor: COLORS.bgColor,
        justifyContent: "center"
    }
})